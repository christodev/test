import {StyleSheet} from 'react-native'

export const styles = StyleSheet.create({
  card: {
    backgroundColor: '#fff',
    elevation: 5,
    padding: 10,
    marginVertical: 10,
    borderRadius: 8,
  },
})
